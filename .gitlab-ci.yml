default:
  image: node:latest

build-job:
  stage: build
  script:
    - npm install

publish:
  stage: deploy
  script:
    - npm set registry  https://registry.npmjs.org/:_authToken=${NPM_TOKEN}
    - npm publish --access public
